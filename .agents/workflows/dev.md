---
description: Start the development server
---

Start the FastAPI development server with hot-reload enabled.

> **NOTE FOR AGENT**: Do NOT run this command yourself. Simply show the user the command below so they can run it in their own terminal.

1. Show the user this command to run in their terminal:
```bash
uv run uvicorn main:app --reload
```
